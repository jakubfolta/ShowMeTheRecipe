export default class Likes {
    constructor () {
        this.likes = [];
    }

    addLike(id, title, author, img) {
        const like = {
            this.likes.push(id, title, author, img)
        }
    }
}